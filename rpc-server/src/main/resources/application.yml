spring:
  application:
    name: rpc-server

# web port
server:
  port: 8000

# 是否打开swagger
swagger:
  enable: true

wlb:
  rpc:
    server:
      port: 11211
      execute:
        enable: true
        factory: CUSTOM
        cache: JDK
      balance:
        enable: true
        arithmetic: ROUND


logging:
  level:
    root: info

